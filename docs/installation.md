# Installation

Install from PyPI:

```bash
pip install pydongo
```

Or with UV:

```bash
uv add pydongo
```

## Requirements

- Python 3.10+
- `pymongo` (auto-installed)
- `pydantic` (auto-installed)
- `motor` (for async support)
