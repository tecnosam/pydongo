## Installation

Install from PyPI:

```bash
pip install pydongo
```

Or with Poetry:

```bash
poetry add pydongo
```

### Requirements
- Python 3.9+
- `pymongo` (auto-installed)
- `motor` (for async support)
